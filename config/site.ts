export type SiteConfig = typeof siteConfig

export const siteConfig = {
  name: "Contact Management",
  description:
    "A simple contact management application built with React, TypeScript, and Tailwind CSS.",
}
